# Case-study research docs

One markdown file per project. These are the source of truth behind
`app/data/case-studies/*.ts` — each has Overview, Architecture (+ JSON
diagram spec), Tech stack, Data model (+ JSON DB spec), "Why this, not
that", Fun facts, Screenshot targets, and a **Gaps** section listing what
only the author can answer.
